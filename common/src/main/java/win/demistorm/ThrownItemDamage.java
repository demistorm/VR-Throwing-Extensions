package win.demistorm;

public enum ThrownItemDamage {
    ON,           // All thrown items deal damage
    WEAPONS_ONLY, // Only weapons deal damage
    MOBS_ONLY     // Thrown items don't damage players
}
